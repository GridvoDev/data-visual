apiVersion: v1
kind: Service
metadata:
  labels:
    name: data-visual
  name: data-visual
  namespace: gridvo
spec:
  selector:
    name: data-visual
  ports:
     - port: 80
       targetPort: 3001
